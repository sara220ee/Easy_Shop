<Window x:Class="EasyShopWindow.AdminManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Gestion des Administrateurs" Height="600" Width="800">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#F0F0F0" Offset="0"/>
            <GradientStop Color="#E5E5E5" Offset="0.3"/>
            <GradientStop Color="#DADADA" Offset="0.7"/>
            <GradientStop Color="#CFCFCF" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Content="Retour" Click="Retour_Click" Width="100" Margin="0,0,30,0" Background="#FF1E3C72" Foreground="White" HorizontalAlignment="Right">
            <Button.Effect>
                <DropShadowEffect Color="Black" Direction="320" ShadowDepth="5" Opacity="0.5"/>
            </Button.Effect>
        </Button>

      
        <DataGrid x:Name="AdminDataGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False">
            <DataGrid.Columns >
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="*" />
                <DataGridTextColumn Header="Nom" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Adresse" Binding="{Binding Address}" Width="*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="auto" />
                <DataGridTextColumn Header="Date de Naissance" Binding="{Binding BirthDate, StringFormat={}{0:dd/MM/yyyy}}" Width="auto"/>
                <DataGridTemplateColumn Header="Actions" Width="2*" >
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Modifier" Click="EditAdmin_Click" Margin="5"/>
                                <Button Content="Supprimer" Click="DeleteAdmin_Click" Margin="5"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="20" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Label Content="Ajouter un Administrateur" Grid.Row="0" FontSize="16" FontWeight="Bold" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,0,0,20" FontFamily="Segoe UI Light"/>
                <Label Content="Nom" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBox x:Name="NameTextBox" Grid.Row="1" Grid.Column="1" Width="200" Margin="0,5,0,5" Text="{Binding Name}"/>
                <Label Content="Adresse" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBox x:Name="AddressTextBox" Grid.Row="2" Grid.Column="1" Width="200" Margin="0,5,0,5" Text="{Binding Address}"/>
                <Label Content="Email" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <TextBox x:Name="EmailTextBox" Grid.Row="3" Grid.Column="1" Width="200" Margin="0,5,0,5" Text="{Binding Email}"/>
                <Label Content="Mot de passe" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="4" Grid.Column="1" Width="200" Margin="0,5,0,5"/>
                <Label Content="Date de naissance" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                <DatePicker x:Name="BirthDatePicker" Grid.Row="5" Grid.Column="1" Width="200" Margin="0,5,0,5" SelectedDate="{Binding BirthDate}"/>

                <Button Content="Sauvgerder les modification" Click="AddAdmin_Click" Grid.Row="6" Grid.ColumnSpan="2" Width="auto" Background="#FF6A11CB" Foreground="White" HorizontalAlignment="Center" Margin="0,20,0,10" Padding="3">
                    <Button.Effect>
                        <DropShadowEffect Color="Black" Direction="320" ShadowDepth="5" Opacity="0.5"/>
                    </Button.Effect>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>